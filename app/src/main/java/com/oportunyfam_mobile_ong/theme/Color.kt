package com.oportunyfam_mobile_ong.theme

import androidx.compose.ui.graphics.Color

/**
 * Cores do Tema OportunyFam
 *
 * Define o esquema de cores para os temas claro e escuro do aplicativo.
 */

// ==================== CORES PRINCIPAIS ====================

/** Laranja dourado principal */
val OrangeGold = Color(0xFFC16512)

/** Laranja dourado brilhante */
val BrightOrangeGold = Color(0xFFFFB301)

/** Branco puro */
val PureWhite = Color(0xFFFFFFFF)

/** Texto escuro padrão */
val DarkText = Color(0xFF1C1B20)

// ==================== TEMA CLARO ====================

/** Cor primária do tema claro */
val primaryLight = Color(0xFFC16512)

/** Cor do texto sobre a cor primária (tema claro) */
val onPrimaryLight = Color(0xFFFFFFFF)

/** Container da cor primária (tema claro) */
val primaryContainerLight = Color(0xFFFFB301)

/** Cor do texto sobre o container primário (tema claro) */
val onPrimaryContainerLight = Color(0xFF363534)

/** Cor secundária (tema claro) */
val secondaryLight = Color(0xFFFFA000)

/** Cor do texto sobre a cor secundária (tema claro) */
val onSecondaryLight = Color(0xFFC5C5C5)

/** Cor de fundo (tema claro) */
val backgroundLight = Color(0xFFFFFFFF)

/** Cor do texto sobre o fundo (tema claro) */
val onBackgroundLight = Color(0xFF1C1B20)

/** Cor da superfície (tema claro) */
val surfaceLight = Color(0xFFFFFFFF)

/** Cor do texto sobre a superfície (tema claro) */
val onSurfaceLight = Color(0xFF1C1B20)

// ==================== TEMA ESCURO ====================

/** Cor primária do tema escuro */
val primaryDark = Color(0xFFFFB301)

/** Cor do texto sobre a cor primária (tema escuro) */
val onPrimaryDark = Color(0xFF1C1B20)

/** Container da cor primária (tema escuro) */
val primaryContainerDark = Color(0xFFC16512)

/** Cor do texto sobre o container primário (tema escuro) */
val onPrimaryContainerDark = Color(0xFFFFFFFF)

/** Cor secundária (tema escuro) */
val secondaryDark = Color(0xFFB35220)

/** Cor do texto sobre a cor secundária (tema escuro) */
val onSecondaryDark = Color(0xFFFFFFFF)

/** Cor de fundo (tema escuro) */
val backgroundDark = Color(0xFF141318)

/** Cor do texto sobre o fundo (tema escuro) */
val onBackgroundDark = Color(0xFFFFFFFF)

/** Cor da superfície (tema escuro) */
val surfaceDark = Color(0xFF1C1B20)

/** Cor do texto sobre a superfície (tema escuro) */
val onSurfaceDark = Color(0xFFFFFFFF)